This file is a merged representation of a subset of the codebase, containing specifically included files, combined into a single document by Repomix.
The content has been processed where comments have been removed.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: Sohba/Controllers/DashboardController.cs, Sohba.Application/Interfaces/IUserService.cs, Sohba.Application/Interfaces/IPostService.cs, Sohba.Application/Interfaces/IGroupService.cs, Sohba.Application/Interfaces/IPageService.cs, Sohba.Application/Interfaces/IReportingService.cs, Sohba.Application/Interfaces/IFriendshipService.cs, Sohba.Application/DTOs/UserAggregate/UserResponseDto.cs, Sohba.Application/DTOs/PostAggregate/PostResponseDto.cs, Sohba.Application/DTOs/PostAggregate/PostReportResponseDto.cs, Sohba/ViewModels/Dashboard/DashboardViewModel.cs, Sohba/ViewModels/Dashboard/DashboardUsersViewModel.cs, Sohba/ViewModels/Dashboard/DashboardPostsViewModel.cs, Sohba/ViewModels/Dashboard/DashboardReportsViewModel.cs, Sohba.Domain/Common/Result.cs, Sohba/Views/Dashboard/Index.cshtml, Sohba/Views/Dashboard/Users.cshtml, Sohba/Views/Dashboard/Posts.cshtml, Sohba/Views/Dashboard/Reports.cshtml, Sohba/Views/Dashboard/Partials/_UserDetails.cshtml, Sohba/Views/Dashboard/Partials/_PostDetails.cshtml, Sohba/Views/Dashboard/Partials/_ReportDetails.cshtml, Sohba/wwwroot/js/features/dashboard.js
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Code comments have been removed from supported file types
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
Sohba/
  Controllers/
    DashboardController.cs
  ViewModels/
    Dashboard/
      DashboardPostsViewModel.cs
      DashboardReportsViewModel.cs
      DashboardUsersViewModel.cs
      DashboardViewModel.cs
  Views/
    Dashboard/
      Partials/
        _PostDetails.cshtml
        _ReportDetails.cshtml
        _UserDetails.cshtml
      Index.cshtml
      Posts.cshtml
      Reports.cshtml
      Users.cshtml
  wwwroot/
    js/
      features/
        dashboard.js
Sohba.Application/
  DTOs/
    PostAggregate/
      PostReportResponseDto.cs
      PostResponseDto.cs
    UserAggregate/
      UserResponseDto.cs
  Interfaces/
    IFriendshipService.cs
    IGroupService.cs
    IPageService.cs
    IPostService.cs
    IReportingService.cs
    IUserService.cs
Sohba.Domain/
  Common/
    Result.cs
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="Sohba/ViewModels/Dashboard/DashboardPostsViewModel.cs">
using Sohba.Application.DTOs.PostAggregate;

namespace Sohba.ViewModels.Dashboard
{
    public class DashboardPostsViewModel
    {
        public List<PostResponseDto> Posts { get; set; } = new();
        public int TotalCount { get; set; }
        public int CurrentPage { get; set; }
        public int PageSize { get; set; } = 20;
        public string SearchTerm { get; set; }
        public string SourceFilter { get; set; } = "all";
    }
}
</file>

<file path="Sohba/ViewModels/Dashboard/DashboardReportsViewModel.cs">
using Sohba.Application.DTOs.PostAggregate;

namespace Sohba.ViewModels.Dashboard
{
    public class DashboardReportsViewModel
    {
        public List<PostReportResponseDto> Reports { get; set; } = new();
        public int TotalCount { get; set; }
        public int CurrentPage { get; set; }
        public int PageSize { get; set; } = 20;
        public string StatusFilter { get; set; } = "pending";
    }
}
</file>

<file path="Sohba/ViewModels/Dashboard/DashboardUsersViewModel.cs">
using Sohba.Application.DTOs.UserAggregate;

namespace Sohba.ViewModels.Dashboard
{
    public class DashboardUsersViewModel
    {
        public List<UserResponseDto> Users { get; set; } = new();
        public int TotalCount { get; set; }
        public int CurrentPage { get; set; }
        public int PageSize { get; set; } = 20;
        public string SearchTerm { get; set; }
        public string StatusFilter { get; set; } = "all";
    }
}
</file>

<file path="Sohba/ViewModels/Dashboard/DashboardViewModel.cs">
using Sohba.Application.DTOs.PostAggregate;
using Sohba.Application.DTOs.UserAggregate;

namespace Sohba.ViewModels.Dashboard
{
    public class DashboardViewModel
    {

        public int TotalUsers { get; set; }
        public int TotalPosts { get; set; }
        public int TotalGroups { get; set; }
        public int TotalPages { get; set; }
        public int PendingReports { get; set; }
        public int NewUsersToday { get; set; }
        public int NewPostsToday { get; set; }


        public List<int> UsersLast7Days { get; set; } = new();
        public List<string> Last7DaysLabels { get; set; } = new();


        public List<UserResponseDto> RecentUsers { get; set; } = new();
        public List<PostResponseDto> RecentPosts { get; set; } = new();
        public List<PostReportResponseDto> RecentReports { get; set; } = new();
    }
}
</file>

<file path="Sohba/Views/Dashboard/Partials/_PostDetails.cshtml">
@model Sohba.Application.DTOs.PostAggregate.PostResponseDto

<div class="p-6">
    <div class="flex items-center gap-4 mb-4">
        @if (!string.IsNullOrEmpty(Model.ImageUrl))
        {
            <img src="@Model.ImageUrl" class="w-20 h-20 rounded-xl object-cover" />
        }
        else
        {
            <div class="w-20 h-20 bg-slate-100 rounded-xl flex items-center justify-center text-slate-400 text-2xl">
                📝
            </div>
        }
        <div>
            <h3 class="text-xl font-black text-gray-900">@Model.Title</h3>
            <p class="text-sm text-gray-500">by @Model.AuthorName</p>
            <p class="text-xs text-gray-400 mt-1">@Model.CreatedAt.ToString("MMMM dd, yyyy HH:mm")</p>
        </div>
    </div>

    <div class="bg-slate-50 p-4 rounded-xl mb-4">
        <p class="text-gray-700 whitespace-pre-line">@Model.Content</p>
    </div>

    <div class="flex items-center gap-6 text-sm">
        <span class="flex items-center gap-1">
            <svg class="w-5 h-5 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
            </svg>
            @Model.ReactionsCount Reactions
        </span>
        <span class="flex items-center gap-1">
            <svg class="w-5 h-5 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
            </svg>
            @Model.CommentsCount Comments
        </span>
        <span class="px-3 py-1 bg-slate-100 text-slate-700 text-xs font-bold rounded-full">
            @Model.SourceType
        </span>
    </div>
</div>
</file>

<file path="Sohba/Views/Dashboard/Partials/_UserDetails.cshtml">
@model Sohba.Application.DTOs.UserAggregate.UserResponseDto

<div class="p-6">
    <div class="flex items-center gap-4 mb-6">
        <img src="@(Model.ProfilePictureUrl ?? $"https://ui-avatars.com/api/?name={Model.Name}&background=345e69&color=fff")"
             class="w-20 h-20 rounded-full object-cover border-4 border-white shadow-lg" />
        <div>
            <h3 class="text-xl font-black text-gray-900">@Model.Name</h3>
            <p class="text-gray-500">@Model.Email</p>
            <p class="text-sm text-gray-400 mt-1">Joined @Model.CreatedAt.ToString("MMMM dd, yyyy")</p>
        </div>
    </div>

    <div class="grid grid-cols-2 gap-4 mb-6">
        <div class="bg-slate-50 p-4 rounded-xl">
            <span class="text-2xl font-black text-[#345e69]">0</span>
            <span class="text-sm text-gray-600 block">Friends</span>
        </div>
        <div class="bg-slate-50 p-4 rounded-xl">
            <span class="text-2xl font-black text-[#345e69]">0</span>
            <span class="text-sm text-gray-600 block">Posts</span>
        </div>
    </div>

    <div class="border-t border-slate-100 pt-4">
        <p class="text-sm text-gray-600"><span class="font-bold">Bio:</span> @(Model.Bio ?? "No bio")</p>
    </div>
</div>
</file>

<file path="Sohba/wwwroot/js/features/dashboard.js">
window.resolveReport = async function (reportId, isResolved) {
    const actionText = isResolved ? 'resolve' : 'dismiss';
    window.showConfirmModal({
        title: isResolved ? 'Resolve Report' : 'Dismiss Report',
        message: `Are you sure you want to ${actionText} this report?`,
        type: isResolved ? 'warning' : 'info',
        confirmText: isResolved ? 'Resolve' : 'Dismiss',
        onConfirm: async () => {
            try {
                const result = await SohbaApp.post('/Dashboard/ResolveReport', { reportId: reportId, isResolved: isResolved });
                if (result.success) {
                    SohbaApp.toast(`Report ${actionText}d successfully.`, 'success');
                    const statusEl = document.getElementById(`report-status-${reportId}`);
                    if (statusEl) {
                        statusEl.textContent = isResolved ? 'Resolved' : 'Dismissed';
                        statusEl.className = isResolved ? 'text-green-600' : 'text-gray-500';
                    }

                    const btnContainer = event.target.closest('.flex.gap-3');
                    if (btnContainer) btnContainer.remove();
                } else {
                    SohbaApp.toast(result.error || `Failed to ${actionText} report.`, 'error');
                }
            } catch (err) {
                SohbaApp.toast('An unexpected error occurred.', 'error');
            }
        }
    });
};
</file>

<file path="Sohba.Application/DTOs/PostAggregate/PostReportResponseDto.cs">
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Application.DTOs.PostAggregate
{
    public class PostReportResponseDto
    {
        public Guid Id { get; set; }
        public Guid PostId { get; set; }
        public string ReporterName { get; set; }
        public string Reason { get; set; }
        public bool IsResolved { get; set; }
        public DateTime ReportedAt { get; set; }
    }
}
</file>

<file path="Sohba.Application/DTOs/UserAggregate/UserResponseDto.cs">
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Application.DTOs.UserAggregate
{
    public class UserResponseDto
    {
        public Guid Id { get; set; }
        public string Name { get; set; }
        public string Email { get; set; }
        public string Bio { get; set; }
        public string? ProfilePictureUrl { get; set; }
        public DateTime CreatedAt { get; set; }
    }
}
</file>

<file path="Sohba/Views/Dashboard/Partials/_ReportDetails.cshtml">
@model Sohba.Application.DTOs.PostAggregate.PostReportResponseDto

<div class="p-6">
    <div class="flex items-center gap-4 mb-6">
        <div class="w-14 h-14 bg-red-100 rounded-2xl flex items-center justify-center">
            <svg class="w-7 h-7 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
        </div>
        <div>
            <h3 class="text-xl font-black text-gray-900">Report Details</h3>
            <p class="text-sm text-gray-500">Reported by @Model.ReporterName</p>
            <p class="text-xs text-gray-400 mt-1">@Model.ReportedAt.ToString("MMMM dd, yyyy HH:mm")</p>
        </div>
    </div>

    <div class="bg-red-50 p-4 rounded-xl mb-4">
        <p class="text-sm text-red-700"><span class="font-bold">Reason:</span> @Model.Reason</p>
    </div>

    <div class="border-t border-slate-100 pt-4">
        <p class="text-sm text-gray-600"><span class="font-bold">Post ID:</span> @Model.PostId</p>
        <p class="text-sm text-gray-600 mt-1">
            <span class="font-bold">Status:</span>
            <span id="report-status-@Model.Id" class="@(Model.IsResolved ? "text-green-600" : "text-red-600")">
                @(Model.IsResolved ? "Resolved" : "Pending")
            </span>
        </p>
    </div>
    @if (!Model.IsResolved)
    {
        <div class="flex gap-3 mt-6 pt-4 border-t border-slate-100">
            <button onclick="resolveReport('@Model.Id', true)" class="flex-1 py-2.5 bg-green-600 hover:bg-green-700 text-white font-semibold rounded-xl shadow-lg shadow-green-600/30 transition-colors">
                Mark as Resolved
            </button>
            <button onclick="resolveReport('@Model.Id', false)" class="flex-1 py-2.5 border border-gray-200 text-gray-600 font-semibold rounded-xl hover:bg-gray-50 transition-colors">
                Dismiss Report
            </button>
        </div>
    }
</div>
</file>

<file path="Sohba/Views/Dashboard/Index.cshtml">
@model Sohba.ViewModels.Dashboard.DashboardViewModel
@{
    ViewData["Title"] = "Dashboard";
    // Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "_AppLayout";
}

<!-- Charts JS -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<div class="max-w-7xl mx-auto page-transition">
    <!-- Header -->
    <div class="bg-white rounded-3xl shadow-sm border border-slate-100 p-6 mb-6">
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-3xl font-black text-gray-900">Dashboard</h1>
                <p class="text-gray-500 mt-1">Welcome back, Admin!</p>
            </div>
            <div class="flex items-center gap-3">
                <span class="px-4 py-2 bg-[#345e69]/10 text-[#345e69] rounded-xl font-semibold text-sm">
                    @DateTime.Now.ToString("dddd, MMMM dd, yyyy")
                </span>
            </div>
        </div>
    </div>

    <!-- Stats Cards -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-5 mb-6">
        <!-- Total Users -->
        <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-5 hover:shadow-lg transition-all duration-300 group">
            <div class="flex items-center justify-between mb-3">
                <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform">
                    <svg class="w-6 h-6 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
                    </svg>
                </div>
                <span class="text-sm font-medium text-green-600 bg-green-50 px-2.5 py-1 rounded-lg">+@(Model.NewUsersToday)</span>
            </div>
            <h3 class="text-2xl font-black text-gray-900">@Model.TotalUsers.ToString("N0")</h3>
            <p class="text-sm text-gray-500 mt-1">Total Users</p>
        </div>

        <!-- Total Posts -->
        <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-5 hover:shadow-lg transition-all duration-300 group">
            <div class="flex items-center justify-between mb-3">
                <div class="w-12 h-12 bg-emerald-100 rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform">
                    <svg class="w-6 h-6 text-emerald-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                    </svg>
                </div>
                <span class="text-sm font-medium text-green-600 bg-green-50 px-2.5 py-1 rounded-lg">+@(Model.NewPostsToday)</span>
            </div>
            <h3 class="text-2xl font-black text-gray-900">@Model.TotalPosts.ToString("N0")</h3>
            <p class="text-sm text-gray-500 mt-1">Total Posts</p>
        </div>

        <!-- Total Groups -->
        <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-5 hover:shadow-lg transition-all duration-300 group">
            <div class="flex items-center justify-between mb-3">
                <div class="w-12 h-12 bg-purple-100 rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform">
                    <svg class="w-6 h-6 text-purple-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                    </svg>
                </div>
                <span class="text-sm font-medium text-green-600 bg-green-50 px-2.5 py-1 rounded-lg">+5%</span>
            </div>
            <h3 class="text-2xl font-black text-gray-900">@Model.TotalGroups.ToString("N0")</h3>
            <p class="text-sm text-gray-500 mt-1">Total Groups</p>
        </div>

        <!-- Total Pages -->
        <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-5 hover:shadow-lg transition-all duration-300 group">
            <div class="flex items-center justify-between mb-3">
                <div class="w-12 h-12 bg-teal-100 rounded-xl flex items-center justify-center group-hover:scale-110 transition-transform">
                    <svg class="w-6 h-6 text-teal-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z" />
                    </svg>
                </div>
            </div>
            <h3 class="text-2xl font-black text-gray-900">@Model.TotalPages.ToString("N0")</h3>
            <p class="text-sm text-gray-500 mt-1">Total Pages</p>
        </div>
    </div>

    <!-- Pending Reports Card (منفصلة) -->
    <div class="bg-gradient-to-r from-red-50 to-orange-50 rounded-2xl border border-red-100 p-6 mb-6">
        <div class="flex items-center justify-between">
            <div class="flex items-center gap-4">
                <div class="w-14 h-14 bg-red-100 rounded-2xl flex items-center justify-center">
                    <svg class="w-7 h-7 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                    </svg>
                </div>
                <div>
                    <h3 class="text-2xl font-black text-gray-900">@Model.PendingReports Pending Reports</h3>
                    <p class="text-red-600">Requires your attention</p>
                </div>
            </div>
            <a asp-action="Reports" class="px-6 py-3 bg-red-600 text-white font-bold rounded-xl hover:bg-red-700 transition-colors">
                View Reports
            </a>
        </div>
    </div>

    <!-- Charts & Recent Activity -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Users Chart (Last 7 Days) -->
        <div class="lg:col-span-2 bg-white rounded-2xl shadow-sm border border-slate-100 p-6">
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-lg font-black text-gray-900">User Activity</h2>
                <select class="px-3 py-1.5 bg-slate-50 border border-slate-200 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-[#345e69]/20">
                    <option>Last 7 days</option>
                    <option>Last 30 days</option>
                    <option>Last 3 months</option>
                </select>
            </div>
            <div class="h-80">
                <canvas id="usersChart"></canvas>
            </div>
        </div>

        <!-- Recent Reports -->
        <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-6">
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-lg font-black text-gray-900">Recent Reports</h2>
                <a asp-action="Reports" class="text-sm text-[#345e69] font-semibold hover:underline">View All</a>
            </div>
            <div class="space-y-4 max-h-96 overflow-y-auto">
                @if (Model.RecentReports.Any())
                {
                    @foreach (var report in Model.RecentReports)
                    {
                        <div class="flex items-start gap-3 p-3 hover:bg-slate-50 rounded-xl transition-colors">
                            <div class="w-8 h-8 bg-red-100 rounded-lg flex items-center justify-center flex-shrink-0">
                                <svg class="w-4 h-4 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                                </svg>
                            </div>
                            <div class="flex-1 min-w-0">
                                <p class="text-sm font-semibold text-gray-900 truncate">@report.Reason</p>
                                <p class="text-xs text-gray-500">Reported @report.ReportedAt.ToString("MMM dd")</p>
                            </div>
                            <span class="text-xs px-2 py-1 bg-red-50 text-red-600 rounded-full">!</span>
                        </div>
                    }
                }
                else
                {
                    <p class="text-sm text-gray-500 text-center py-4">No pending reports</p>
                }
            </div>
        </div>

        <!-- Recent Users -->
        <div class="lg:col-span-2 bg-white rounded-2xl shadow-sm border border-slate-100 p-6">
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-lg font-black text-gray-900">Recent Users</h2>
                <a asp-action="Users" class="text-sm text-[#345e69] font-semibold hover:underline">View All</a>
            </div>
            <div class="space-y-4 max-h-96 overflow-y-auto">
                @if (Model.RecentUsers.Any())
                {
                    @foreach (var user in Model.RecentUsers)
                    {
                        <div class="flex items-center justify-between p-3 hover:bg-slate-50 rounded-xl transition-colors">
                            <div class="flex items-center gap-3">
                                <img src="@(user.ProfilePictureUrl ?? $"https://ui-avatars.com/api/?name={user.Name}&background=345e69&color=fff")"
                                     class="w-10 h-10 rounded-full object-cover" />
                                <div>
                                    <h3 class="font-semibold text-gray-900">@user.Name</h3>
                                    <p class="text-xs text-gray-500">@user.Email</p>
                                </div>
                            </div>
                            <span class="text-xs text-gray-400">@user.CreatedAt.ToString("MMM dd")</span>
                        </div>
                    }
                }
                else
                {
                    <p class="text-sm text-gray-500 text-center py-4">No users yet</p>
                }
            </div>
        </div>

        <!-- Recent Posts -->
        <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-6">
            <div class="flex items-center justify-between mb-4">
                <h2 class="text-lg font-black text-gray-900">Recent Posts</h2>
                <a asp-action="Posts" class="text-sm text-[#345e69] font-semibold hover:underline">View All</a>
            </div>
            <div class="space-y-4 max-h-96 overflow-y-auto">
                @if (Model.RecentPosts.Any())
                {
                    @foreach (var post in Model.RecentPosts)
                    {
                        <div class="flex items-start gap-3 p-3 hover:bg-slate-50 rounded-xl transition-colors">
                            @if (!string.IsNullOrEmpty(post.ImageUrl))
                            {
                                <img src="@post.ImageUrl" class="w-10 h-10 rounded-lg object-cover" />
                            }
                            else
                            {
                                <div class="w-10 h-10 bg-slate-100 rounded-lg flex items-center justify-center text-slate-400">
                                    📝
                                </div>
                            }
                            <div class="flex-1 min-w-0">
                                <p class="text-sm font-semibold text-gray-900 truncate">@post.Title</p>
                                <p class="text-xs text-gray-500">by @post.AuthorName · @post.CreatedAt.ToString("MMM dd")</p>
                            </div>
                        </div>
                    }
                }
                else
                {
                    <p class="text-sm text-gray-500 text-center py-4">No posts yet</p>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Initialize Chart
        const ctx = document.getElementById('usersChart').getContext('2d');
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: @Html.Raw(Json.Serialize(Model.Last7DaysLabels)),
                datasets: [{
                    label: 'New Users',
                    data: @Html.Raw(Json.Serialize(Model.UsersLast7Days)),
                    borderColor: '#345e69',
                    backgroundColor: 'rgba(52, 94, 105, 0.1)',
                    borderWidth: 3,
                    pointBackgroundColor: '#345e69',
                    pointBorderColor: '#fff',
                    pointBorderWidth: 2,
                    pointRadius: 5,
                    pointHoverRadius: 7,
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        grid: {
                            color: '#f1f5f9'
                        }
                    },
                    x: {
                        grid: {
                            display: false
                        }
                    }
                }
            }
        });
    </script>
}
</file>

<file path="Sohba/Views/Dashboard/Posts.cshtml">
@model Sohba.ViewModels.Dashboard.DashboardPostsViewModel
@{
    ViewData["Title"] = "Manage Posts";
    // Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "_AppLayout";
}

<div class="max-w-7xl mx-auto page-transition">
    <!-- Header -->
    <div class="bg-white rounded-3xl shadow-sm border border-slate-100 p-6 mb-6">
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-3xl font-black text-gray-900">Posts</h1>
                <p class="text-gray-500 mt-1">Manage all posts in the platform</p>
            </div>
            <div class="text-sm text-gray-500">
                Total: <span class="font-bold text-gray-900">@Model.TotalCount</span>
            </div>
        </div>

        <!-- Filters -->
        <div class="flex flex-wrap gap-4 mt-6">
            <div class="flex-1 min-w-[200px]">
                <div class="relative">
                    <input type="text"
                           id="searchInput"
                           value="@Model.SearchTerm"
                           placeholder="Search by title or content..."
                           class="w-full pl-10 pr-4 py-2 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-[#345e69]/20" />
                    <span class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400">
                        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                        </svg>
                    </span>
                </div>
            </div>
            <select id="sourceFilter" class="px-4 py-2 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-[#345e69]/20">
                <option value="all" selected="@(Model.SourceFilter == "all")">All Sources</option>
                <option value="User" selected="@(Model.SourceFilter == "User")">Users</option>
                <option value="Group" selected="@(Model.SourceFilter == "Group")">Groups</option>
                <option value="Page" selected="@(Model.SourceFilter == "Page")">Pages</option>
            </select>
            <button onclick="applyFilters()" class="px-6 py-2 bg-[#345e69] text-white font-semibold rounded-xl hover:bg-[#2a4b55] transition-colors">
                Apply Filters
            </button>
        </div>
    </div>

    <!-- Posts Table -->
    <div class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-slate-50 border-b border-slate-200">
                    <tr>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Post</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Author</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Source</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Date</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Stats</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    @if (Model.Posts.Any())
                    {
                        @foreach (var post in Model.Posts)
                        {
                            <tr class="hover:bg-slate-50 transition-colors" data-post-id="@post.Id">
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-3">
                                        @if (!string.IsNullOrEmpty(post.ImageUrl))
                                        {
                                            <img src="@post.ImageUrl" class="w-12 h-12 rounded-lg object-cover" />
                                        }
                                        else
                                        {
                                            <div class="w-12 h-12 bg-slate-100 rounded-lg flex items-center justify-center text-slate-400">
                                                📝
                                            </div>
                                        }
                                        <div>
                                            <div class="font-semibold text-gray-900">@post.Title</div>
                                            <div class="text-xs text-gray-500 line-clamp-1">@post.Content</div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-2">
                                        <img src="https://ui-avatars.com/api/?name=@post.AuthorName&background=345e69&color=fff"
                                             class="w-6 h-6 rounded-full" />
                                        <span class="text-sm text-gray-600">@post.AuthorName</span>
                                    </div>
                                </td>
                                <td class="px-6 py-4">
                                    <span class="px-3 py-1 bg-slate-100 text-slate-700 text-xs font-bold rounded-full">
                                        @post.SourceType
                                    </span>
                                </td>
                                <td class="px-6 py-4 text-sm text-gray-600">@post.CreatedAt.ToString("MMM dd, yyyy")</td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-3 text-xs">
                                        <span class="flex items-center gap-1">
                                            <svg class="w-4 h-4 text-blue-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
                                            </svg>
                                            @post.ReactionsCount
                                        </span>
                                        <span class="flex items-center gap-1">
                                            <svg class="w-4 h-4 text-green-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
                                            </svg>
                                            @post.CommentsCount
                                        </span>
                                    </div>
                                </td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-2">
                                        <button onclick="viewPost('@post.Id')" class="p-2 text-blue-600 hover:bg-blue-50 rounded-xl transition-colors" title="View">
                                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0zM2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                            </svg>
                                        </button>
                                        <button onclick="hidePost('@post.Id')" class="p-2 text-orange-600 hover:bg-orange-50 rounded-xl transition-colors" title="Hide">
                                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 011.563-3.029m5.858.908a3 3 0 114.243 4.243M9.878 9.878l4.242 4.242M9.88 9.88l-3.29-3.29m7.532 7.532l3.29 3.29M3 3l3.59 3.59m0 0A9.953 9.953 0 0112 5c4.478 0 8.268 2.943 9.543 7a10.025 10.025 0 01-4.132 5.411m0 0L21 21" />
                                            </svg>
                                        </button>
                                        <button onclick="deletePost('@post.Id')" class="p-2 text-red-600 hover:bg-red-50 rounded-xl transition-colors" title="Delete">
                                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                            </svg>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="px-6 py-12 text-center text-gray-500">
                                No posts found
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        @if (Model.TotalCount > Model.PageSize)
        {
            <div class="px-6 py-4 border-t border-slate-100 flex items-center justify-between">
                <div class="text-sm text-gray-500">
                    Showing @(((Model.CurrentPage - 1) * Model.PageSize) + 1) to @(Math.Min(Model.CurrentPage * Model.PageSize, Model.TotalCount)) of @Model.TotalCount posts
                </div>
                <div class="flex gap-2">
                    @if (Model.CurrentPage > 1)
                    {
                        <a asp-action="Posts" asp-route-page="@(Model.CurrentPage - 1)" asp-route-search="@Model.SearchTerm" asp-route-source="@Model.SourceFilter"
                           class="px-4 py-2 border border-slate-200 rounded-xl hover:bg-slate-50 transition-colors">
                            Previous
                        </a>
                    }
                    @if (Model.CurrentPage < (int)Math.Ceiling((double)Model.TotalCount / Model.PageSize))
                    {
                        <a asp-action="Posts" asp-route-page="@(Model.CurrentPage + 1)" asp-route-search="@Model.SearchTerm" asp-route-source="@Model.SourceFilter"
                           class="px-4 py-2 border border-slate-200 rounded-xl hover:bg-slate-50 transition-colors">
                            Next
                        </a>
                    }
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
               (function () {
            var searchTimeout;

            document.addEventListener('DOMContentLoaded', function () {
                const searchInput = document.getElementById('searchInput');
                const sourceFilter = document.getElementById('sourceFilter');

                if (searchInput) {
                    searchInput.addEventListener('input', function () {
                        clearTimeout(searchTimeout);
                        searchTimeout = setTimeout(function () {
                            window.applyFilters();
                        }, 500);
                    });
                }

                if (sourceFilter) {
                    sourceFilter.addEventListener('change', function () {
                        window.applyFilters();
                    });
                }
            });

            // =========================
            // Filters
            // =========================
            window.applyFilters = function () {
                const search = document.getElementById('searchInput')?.value || '';
                const source = document.getElementById('sourceFilter')?.value || 'all';
                window.location.href = `/Dashboard/Posts?search=${encodeURIComponent(search)}&source=${source}`;
            };

            // =========================
            // View Post
            // =========================
            window.viewPost = function (postId) {
                if (!postId) return;
                window.location.href = `/Posts/Details/${postId}`;
            };

            // =========================
            // Hide Post with Modal
            // =========================
            window.hidePost = async function (postId) {
                if (!postId) return;

                showConfirmModal({
                    title: 'Hide Post',
                    message: 'Are you sure you want to hide this post? It will no longer be visible to users.',
                    type: 'warning',
                    confirmText: 'Hide Post',
                    onConfirm: async function() {
                        try {
                            const result = await SohbaApp.post('/Dashboard/HidePost', { postId });

                            if (!result.success) {
                                SohbaApp.toast(result.error || 'Failed to hide post', 'error');
                                return;
                            }

                            SohbaApp.toast('Post hidden successfully', 'success');

                            const row = document.querySelector(`tr[data-post-id="${postId}"]`);
                            if (!row) return;

                            row.classList.add('opacity-50', 'bg-slate-50');

                            const sourceCell = row.querySelector('td:nth-child(3)');
                            if (sourceCell && !sourceCell.querySelector('.hidden-badge')) {
                                sourceCell.insertAdjacentHTML(
                                    'beforeend',
                                    '<span class="hidden-badge ml-2 px-2 py-1 bg-orange-100 text-orange-700 text-xs rounded-full">Hidden</span>'
                                );
                            }

                            const hideBtn = row.querySelector('button[onclick*="hidePost"]');
                            if (hideBtn) {
                                hideBtn.disabled = true;
                                hideBtn.classList.add('opacity-50', 'cursor-not-allowed');
                            }

                        } catch (err) {
                            console.error(err);
                            SohbaApp.toast('Unexpected error occurred', 'error');
                        }
                    }
                });
            };

            // =========================
            // Delete Post with Modal
            // =========================
            window.deletePost = async function (postId) {
                if (!postId) return;

                showConfirmModal({
                    title: 'Delete Post',
                    message: 'Are you sure you want to delete this post? This action cannot be undone.',
                    type: 'delete',
                    confirmText: 'Delete Post',
                    onConfirm: async function() {
                        try {
                            const result = await SohbaApp.post('/Dashboard/DeletePost', { postId });

                            if (!result.success) {
                                SohbaApp.toast(result.error || 'Failed to delete post', 'error');
                                return;
                            }

                            SohbaApp.toast('Post deleted successfully', 'success');

                            const row = document.querySelector(`tr[data-post-id="${postId}"]`);
                            if (row) row.remove();

                        } catch (err) {
                            console.error(err);
                            SohbaApp.toast('Unexpected error occurred', 'error');
                        }
                    }
                });
            };
        })();
    </script>
}
</file>

<file path="Sohba/Views/Dashboard/Reports.cshtml">
@model Sohba.ViewModels.Dashboard.DashboardReportsViewModel
@{
    ViewData["Title"] = "Manage Reports";
    // Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "_AppLayout";
}

<div class="max-w-7xl mx-auto page-transition">
    <!-- Header -->
    <div class="bg-white rounded-3xl shadow-sm border border-slate-100 p-6 mb-6">
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-3xl font-black text-gray-900">Reports</h1>
                <p class="text-gray-500 mt-1">Manage user reports</p>
            </div>
            <div class="text-sm text-gray-500">
                Total: <span class="font-bold text-gray-900">@Model.TotalCount</span>
            </div>
        </div>

        <!-- Filters -->
        <div class="flex flex-wrap gap-4 mt-6">
            <select id="statusFilter" class="px-4 py-2 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-[#345e69]/20">
                <option value="pending" selected="@(Model.StatusFilter == "pending")">Pending Reports</option>
                <option value="resolved" selected="@(Model.StatusFilter == "resolved")">Resolved Reports</option>
                <option value="all" selected="@(Model.StatusFilter == "all")">All Reports</option>
            </select>
            <button onclick="applyFilters()" class="px-6 py-2 bg-[#345e69] text-white font-semibold rounded-xl hover:bg-[#2a4b55] transition-colors">
                Apply Filters
            </button>
        </div>
    </div>

    <!-- Reports Table -->
    <div class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-slate-50 border-b border-slate-200">
                    <tr>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Reported Post</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Reporter</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Reason</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Date</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Status</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    @if (Model.Reports.Any())
                    {
                        @foreach (var report in Model.Reports)
                        {
                            <tr class="hover:bg-slate-50 transition-colors" data-report-id="@report.Id">
                                <td class="px-6 py-4">
                                    <a asp-controller="Posts" asp-action="Details" asp-route-id="@report.PostId" class="text-sm text-blue-600 hover:underline">
                                        Post @report.PostId.ToString().Substring(0, 8)...
                                    </a>
                                </td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-2">
                                        <img src="https://ui-avatars.com/api/?name=@report.ReporterName&background=345e69&color=fff"
                                             class="w-6 h-6 rounded-full" />
                                        <span class="text-sm text-gray-600">@report.ReporterName</span>
                                    </div>
                                </td>
                                <td class="px-6 py-4">
                                    <span class="px-3 py-1 bg-red-50 text-red-700 text-xs font-bold rounded-full">
                                        @report.Reason
                                    </span>
                                </td>
                                <td class="px-6 py-4 text-sm text-gray-600">@report.ReportedAt.ToString("MMM dd, yyyy HH:mm")</td>
                                <td class="px-6 py-4">
                                    @if (report.IsResolved)
                                    {
                                        <span class="px-3 py-1 bg-green-100 text-green-700 text-xs font-bold rounded-full">Resolved</span>
                                    }
                                    else
                                    {
                                        <span class="px-3 py-1 bg-red-100 text-red-700 text-xs font-bold rounded-full">Pending</span>
                                    }
                                </td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-2">
                                        @if (!report.IsResolved)
                                        {
                                            <button onclick="resolveReport('@report.Id')" class="p-2 text-green-600 hover:bg-green-50 rounded-xl transition-colors" title="Resolve">
                                                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                                                </svg>
                                            </button>
                                            <button onclick="dismissReport('@report.Id')" class="p-2 text-gray-600 hover:bg-gray-50 rounded-xl transition-colors" title="Dismiss">
                                                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                                                </svg>
                                            </button>
                                            <button onclick="deleteReportedPost('@report.PostId', '@report.Id')" class="p-2 text-red-600 hover:bg-red-50 rounded-xl transition-colors" title="Delete Post">
                                                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                                </svg>
                                            </button>
                                        }
                                        else
                                        {
                                            <span class="text-xs text-gray-400">No actions</span>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="px-6 py-12 text-center text-gray-500">
                                No reports found
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        @if (Model.TotalCount > Model.PageSize)
        {
            <div class="px-6 py-4 border-t border-slate-100 flex items-center justify-between">
                <div class="text-sm text-gray-500">
                    Showing @(((Model.CurrentPage - 1) * Model.PageSize) + 1) to @(Math.Min(Model.CurrentPage * Model.PageSize, Model.TotalCount)) of @Model.TotalCount reports
                </div>
                <div class="flex gap-2">
                    @if (Model.CurrentPage > 1)
                    {
                        <a asp-action="Reports" asp-route-page="@(Model.CurrentPage - 1)" asp-route-status="@Model.StatusFilter"
                           class="px-4 py-2 border border-slate-200 rounded-xl hover:bg-slate-50 transition-colors">
                            Previous
                        </a>
                    }
                    @if (Model.CurrentPage < (int)Math.Ceiling((double)Model.TotalCount / Model.PageSize))
                    {
                        <a asp-action="Reports" asp-route-page="@(Model.CurrentPage + 1)" asp-route-status="@Model.StatusFilter"
                           class="px-4 py-2 border border-slate-200 rounded-xl hover:bg-slate-50 transition-colors">
                            Next
                        </a>
                    }
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('statusFilter').addEventListener('change', function() {
            applyFilters();
        });

        function applyFilters() {
            const status = document.getElementById('statusFilter').value;
            window.location.href = `/Dashboard/Reports?status=${status}`;
        }

        async function resolveReport(reportId) {
            if (!confirm('Mark this report as resolved?')) return;

            const result = await SohbaApp.post('/Dashboard/ResolveReport', { reportId });
            if (result.success) {
                SohbaApp.toast('Report resolved', 'success');
                location.reload();
            } else {
                SohbaApp.toast(result.error || 'Failed to resolve report', 'error');
            }
        }

        async function dismissReport(reportId) {
            if (!confirm('Dismiss this report?')) return;

            const result = await SohbaApp.post('/Dashboard/DismissReport', { reportId });
            if (result.success) {
                SohbaApp.toast('Report dismissed', 'success');
                location.reload();
            } else {
                SohbaApp.toast(result.error || 'Failed to dismiss report', 'error');
            }
        }

        async function deleteReportedPost(postId, reportId) {
            if (!confirm('Are you sure you want to delete this post? This action cannot be undone.')) return;

            const result = await SohbaApp.post('/Dashboard/DeleteReportedPost', { postId, reportId });
            if (result.success) {
                SohbaApp.toast('Post deleted and report resolved', 'success');
                location.reload();
            } else {
                SohbaApp.toast(result.error || 'Failed to delete post', 'error');
            }
        }
    </script>
}
</file>

<file path="Sohba.Domain/Common/Result.cs">
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Domain.Common
{
    public class Result
    {
        public bool IsSuccess { get; }
        public string Error { get; }
        public bool IsFailure => !IsSuccess;

        protected Result(bool isSuccess, string error)
        {
            if (isSuccess && !string.IsNullOrEmpty(error))
                throw new InvalidOperationException("Success result cannot have an error message.");

            if (!isSuccess && string.IsNullOrEmpty(error))
                throw new InvalidOperationException("Failure result must have an error message.");

            IsSuccess = isSuccess;
            Error = error;
        }

        public static Result Success() => new Result(true, string.Empty);
        public static Result Failure(string error) => new Result(false, error);
    }

    public class Result<T> : Result
    {
        private readonly T _value;

        public T Value => IsSuccess
            ? _value
            : throw new InvalidOperationException("The value of a failure result cannot be accessed.");

        protected Result(T value, bool isSuccess, string error)
            : base(isSuccess, error)
        {
            _value = value;
        }

        public static Result<T> Success(T value) => new Result<T>(value, true, string.Empty);

        public new static Result<T> Failure(string error) => new Result<T>(default, false, error);
    }
}
</file>

<file path="Sohba/Views/Dashboard/Users.cshtml">
@model Sohba.ViewModels.Dashboard.DashboardUsersViewModel
@{
    ViewData["Title"] = "Manage Users";
    // Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = "_AppLayout";
}

<div class="max-w-7xl mx-auto page-transition">
    <!-- Header -->
    <div class="bg-white rounded-3xl shadow-sm border border-slate-100 p-6 mb-6">
        <div class="flex items-center justify-between">
            <div>
                <h1 class="text-3xl font-black text-gray-900">Users</h1>
                <p class="text-gray-500 mt-1">Manage all users in the platform</p>
            </div>
            <div class="text-sm text-gray-500">
                Total: <span class="font-bold text-gray-900">@Model.TotalCount</span>
            </div>
        </div>

        <!-- Filters -->
        <div class="flex flex-wrap gap-4 mt-6">
            <div class="flex-1 min-w-[200px]">
                <div class="relative">
                    <input type="text"
                           id="searchInput"
                           value="@Model.SearchTerm"
                           placeholder="Search by name or email..."
                           class="w-full pl-10 pr-4 py-2 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-[#345e69]/20" />
                    <span class="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400">
                        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                        </svg>
                    </span>
                </div>
            </div>
            <select id="statusFilter" class="px-4 py-2 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-[#345e69]/20">
                <option value="all" selected="@(Model.StatusFilter == "all")">All Users</option>
                <option value="active" selected="@(Model.StatusFilter == "active")">Active</option>
                <option value="blocked" selected="@(Model.StatusFilter == "blocked")">Blocked</option>
            </select>
            <button onclick="applyFilters()" class="px-6 py-2 bg-[#345e69] text-white font-semibold rounded-xl hover:bg-[#2a4b55] transition-colors">
                Apply Filters
            </button>
        </div>
    </div>

    <!-- Users Table -->
    <div class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-slate-50 border-b border-slate-200">
                    <tr>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">User</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Email</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Joined</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Status</th>
                        <th class="px-6 py-4 text-left text-sm font-bold text-gray-600">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    @if (Model.Users.Any())
                    {
                        @foreach (var user in Model.Users)
                        {
                            <tr class="hover:bg-slate-50 transition-colors" data-user-id="@user.Id">
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-3">
                                        <img src="@(user.ProfilePictureUrl ?? $"https://ui-avatars.com/api/?name={user.Name}&background=345e69&color=fff")"
                                             class="w-10 h-10 rounded-full object-cover" />
                                        <div>
                                            <div class="font-semibold text-gray-900">@user.Name</div>
                                            <div class="text-xs text-gray-500">ID: @user.Id.ToString().Substring(0, 8)...</div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-6 py-4 text-sm text-gray-600">@user.Email</td>
                                <td class="px-6 py-4 text-sm text-gray-600">@user.CreatedAt.ToString("MMM dd, yyyy")</td>
                                <td class="px-6 py-4">
                                    <span class="status-badge px-3 py-1 bg-green-100 text-green-700 text-xs font-bold rounded-full">Active</span>
                                </td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center gap-2">
                                        <button onclick="viewUser('@user.Id')" class="p-2 text-blue-600 hover:bg-blue-50 rounded-xl transition-colors" title="View">
                                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0zM2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                            </svg>
                                        </button>
                                        <button onclick="blockUser('@user.Id')" class="p-2 text-orange-600 hover:bg-orange-50 rounded-xl transition-colors" title="Block">
                                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728A9 9 0 015.636 5.636m12.728 12.728L5.636 5.636" />
                                            </svg>
                                        </button>
                                        <button onclick="deleteUser('@user.Id')" class="p-2 text-red-600 hover:bg-red-50 rounded-xl transition-colors" title="Delete">
                                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                            </svg>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="px-6 py-12 text-center text-gray-500">
                                No users found
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        @if (Model.TotalCount > Model.PageSize)
        {
            <div class="px-6 py-4 border-t border-slate-100 flex items-center justify-between">
                <div class="text-sm text-gray-500">
                    Showing @(((Model.CurrentPage - 1) * Model.PageSize) + 1) to @(Math.Min(Model.CurrentPage * Model.PageSize, Model.TotalCount)) of @Model.TotalCount users
                </div>
                <div class="flex gap-2">
                    @if (Model.CurrentPage > 1)
                    {
                        <a asp-action="Users" asp-route-page="@(Model.CurrentPage - 1)" asp-route-search="@Model.SearchTerm" asp-route-status="@Model.StatusFilter"
                           class="px-4 py-2 border border-slate-200 rounded-xl hover:bg-slate-50 transition-colors">
                            Previous
                        </a>
                    }
                    @if (Model.CurrentPage < (int)Math.Ceiling((double)Model.TotalCount / Model.PageSize))
                    {
                        <a asp-action="Users" asp-route-page="@(Model.CurrentPage + 1)" asp-route-search="@Model.SearchTerm" asp-route-status="@Model.StatusFilter"
                           class="px-4 py-2 border border-slate-200 rounded-xl hover:bg-slate-50 transition-colors">
                            Next
                        </a>
                    }
                </div>
            </div>
        }
    </div>
</div>

<!-- User Details Modal -->
<div id="userModal" class="fixed inset-0 z-[200] hidden">
    <div class="absolute inset-0 bg-black/60" onclick="closeUserModal()"></div>
    <div class="absolute inset-0 flex items-center justify-center p-4">
        <div class="bg-white w-full max-w-lg rounded-2xl shadow-2xl overflow-hidden">
            <div class="flex items-center justify-between p-4 border-b">
                <h3 class="text-lg font-bold text-gray-900">User Details</h3>
                <button onclick="closeUserModal()" class="p-1 text-gray-400 hover:text-gray-600">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
            <div id="userModalContent" class="max-h-[70vh] overflow-y-auto">
                <!-- Content will be loaded here -->
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        (function () {
            
            var searchTimeout;

            document.addEventListener('DOMContentLoaded', function () {

                const searchInput = document.getElementById('searchInput');
                const statusFilter = document.getElementById('statusFilter');

                if (searchInput) {
                    searchInput.addEventListener('input', function () {
                        clearTimeout(searchTimeout);
                        searchTimeout = setTimeout(function () {
                            window.applyFilters();
                        }, 500);
                    });
                }

                if (statusFilter) {
                    statusFilter.addEventListener('change', function () {
                        window.applyFilters();
                    });
                }

            });

            // =========================
            // Filters
            // =========================
            window.applyFilters = function () {
                const search = document.getElementById('searchInput')?.value || '';
                const status = document.getElementById('statusFilter')?.value || '';

                window.location.href =
                    `/Dashboard/Users?search=${encodeURIComponent(search)}&status=${status}`;
            };

            // =========================
            // Block User
            // =========================
            asyncwindow.blockUser = async function (userId) {
                showConfirmModal({
                    title: 'Block User',
                    message: 'Are you sure you want to block this user? They will not be able to interact with the platform.',
                    type: 'warning',
                    confirmText: 'Block User',
                    onConfirm: async function() {
                        const result = await SohbaApp.post('/Dashboard/BlockUser', { userId });
                        if (result.success) {
                            SohbaApp.toast('User blocked successfully', 'success');

                            const badge = document.querySelector(`tr[data-user-id="${userId}"] .status-badge`);
                            badge.textContent = 'Blocked';
                            badge.className = 'status-badge px-3 py-1 bg-orange-100 text-orange-700 text-xs font-bold rounded-full';

                            const blockBtn = document.querySelector(`tr[data-user-id="${userId}"] button[onclick*="blockUser"]`);
                            blockBtn.outerHTML = `
                                <button onclick="unblockUser('${userId}')" class="p-2 text-green-600 hover:bg-green-50 rounded-xl transition-colors" title="Unblock">
                                    <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728A9 9 0 015.636 5.636m12.728 12.728L5.636 5.636" />
                                    </svg>
                                </button>
                            `;
                        } else {
                            SohbaApp.toast(result.error || 'Failed to block user', 'error');
                        }
                    }
                });
            };

            // =========================
            // Unblock User
            // =========================
            window.unblockUser = async function (userId) {
                showConfirmModal({
                    title: 'Unblock User',
                    message: 'Are you sure you want to unblock this user? They will be able to interact with the platform again.',
                    type: 'info',
                    confirmText: 'Unblock User',
                    onConfirm: async function() {
                        const result = await SohbaApp.post('/Dashboard/UnblockUser', { userId });
                        if (result.success) {
                            SohbaApp.toast('User unblocked successfully', 'success');
                            const badge = document.querySelector(`tr[data-user-id="${userId}"] .status-badge`);
                            badge.textContent = 'Active';
                            badge.className = 'status-badge px-3 py-1 bg-green-100 text-green-700 text-xs font-bold rounded-full';
                        } else {
                            SohbaApp.toast(result.error || 'Failed to unblock user', 'error');
                        }
                    }
                });
            };

            // =========================
            // Delete User
            // =========================
            window.deleteUser = async function (userId) {
                showConfirmModal({
                    title: 'Delete User',
                    message: 'Are you sure you want to delete this user? This action cannot be undone.',
                    type: 'delete',
                    confirmText: 'Delete User',
                    onConfirm: async function() {
                        const result = await SohbaApp.post('/Dashboard/DeleteUser', { userId });
                        if (result.success) {
                            SohbaApp.toast('User deleted successfully', 'success');
                            document.querySelector(`tr[data-user-id="${userId}"]`).remove();
                        } else {
                            SohbaApp.toast(result.error || 'Failed to delete user', 'error');
                        }
                    }
                });
            };

            // =========================
            // View User
            // =========================
            window.viewUser = async function (userId) {
                try {
                    const response = await fetch(`/Dashboard/GetUserDetails?userId=${userId}`);
                    const html = await response.text();

                    const modal = document.getElementById('userModal');
                    const content = document.getElementById('userModalContent');

                    if (!modal || !content) return;

                    content.innerHTML = html;
                    modal.classList.remove('hidden');
                    document.body.style.overflow = 'hidden';

                } catch (error) {
                    console.error(error);
                    SohbaApp.toast('Failed to load user details', 'error');
                }
            };

            window.closeUserModal = function () {
                const modal = document.getElementById('userModal');
                if (modal) modal.classList.add('hidden');
                document.body.style.overflow = '';
            };

        })();
    </script>
}
</file>

<file path="Sohba.Application/Interfaces/IFriendshipService.cs">
using Sohba.Application.DTOs.UserAggregate;
using Sohba.Domain.Common;
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Application.Interfaces
{
    public interface IFriendshipService
    {

        Task<Result> SendFriendRequestAsync(Guid senderId, Guid receiverId);
        Task<Result> AcceptFriendRequestAsync(Guid senderId, Guid receiverId);
        Task<Result> RejectFriendRequestAsync(Guid senderId, Guid receiverId);
        Task<Result> CancelFriendRequestAsync(Guid senderId, Guid receiverId);


        Task<Result> UnfriendAsync(Guid userId, Guid friendId);
        Task<Result<IEnumerable<FriendDto>>> GetFriendsListAsync(Guid userId);
        Task<bool> AreFriendsAsync(Guid userId, Guid friendId);
        Task<bool> HasPendingRequestAsync(Guid senderId, Guid receiverId);

        Task<Result<IEnumerable<FriendDto>>> GetPendingRequestsAsync(Guid userId);
        Task<Result<IEnumerable<FriendDto>>> GetSentRequestsAsync(Guid userId);
        Task<Result<int>> GetPendingRequestsCountAsync(Guid userId);


        Task<Result> BlockUserAsync(Guid userId, Guid targetId);
        Task<Result> UnblockUserAsync(Guid userId, Guid targetId);
        Task<Result<IEnumerable<FriendDto>>> GetBlockedUsersAsync(Guid userId);


        Task<Result<IEnumerable<UserResponseDto>>> GetFriendSuggestionsAsync(Guid userId, int count = 10);

    }
}
</file>

<file path="Sohba.Application/Interfaces/IGroupService.cs">
using Sohba.Application.DTOs.GroupAndPageAggregate;
using Sohba.Domain.Common;
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Application.Interfaces
{
    public interface IGroupService
    {

        Task<Result<IEnumerable<GroupResponseDto>>> GetAllGroupsAsync(Guid? currentUserId = null);
        Task<Result<IEnumerable<GroupResponseDto>>> GetUserGroupsAsync(Guid userId);
        Task<Result<GroupResponseDto>> GetGroupByIdAsync(Guid groupId);
        Task<Result<GroupResponseDto>> CreateGroupAsync(GroupCreateDto groupDto, Guid adminId);
        Task<Result<bool>> DeleteGroupAsync(Guid groupId, Guid userId);
        Task<Result<GroupResponseDto>> UpdateGroupAsync(GroupUpdateDto updateDto, Guid userId);
        Task<Result<IEnumerable<GroupResponseDto>>> GetRecommendedGroupsAsync(Guid userId, int count = 5);


        Task<Result<bool>> JoinGroupAsync(Guid groupId, Guid userId);
        Task<Result<bool>> LeaveGroupAsync(Guid groupId, Guid userId);
        Task<Result<IEnumerable<GroupMemberDto>>> GetGroupMembersAsync(Guid groupId);


        Task<Result<bool>> KickMemberAsync(Guid groupId, Guid targetUserId, Guid adminId);
    }
}
</file>

<file path="Sohba.Application/Interfaces/IPageService.cs">
using Sohba.Application.DTOs.GroupAndPageAggregate;
using Sohba.Domain.Common;
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Application.Interfaces
{
    public interface IPageService
    {
        Task<Result<PageResponseDto>> CreatePageAsync(Guid adminId, PageCreateDto dto);
        Task<Result> FollowPageAsync(Guid userId, Guid pageId);
        Task<Result> UnfollowPageAsync(Guid userId, Guid pageId);
        Task<Result<PageResponseDto>> GetPageByIdAsync(Guid pageId);
        Task<Result<IEnumerable<PageResponseDto>>> GetUserFollowedPagesAsync(Guid userId);
        Task<Result<IEnumerable<PageResponseDto>>> GetAllPagesAsync();
        Task<Result> DeletePageAsync(Guid adminId, Guid pageId);
        Task<Result<bool>> ToggleFollowPageAsync(Guid userId, Guid pageId);
        Task<Result<bool>> IsFollowingAsync(Guid userId, Guid pageId);
        Task<Result<int>> GetFollowersCountAsync(Guid pageId);
        Task<Result<IEnumerable<PageFollowerDto>>> GetFollowersAsync(Guid pageId, int page = 1, int pageSize = 20);
        Task<Result<PageResponseDto>> UpdatePageAsync(PageUpdateDto updateDto, Guid userId);

    }
}
</file>

<file path="Sohba.Application/Interfaces/IReportingService.cs">
using Sohba.Application.DTOs.PostAggregate;
using Sohba.Domain.Common;
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Application.Interfaces
{
    public interface IReportingService
    {
        Task<Result> ReportPostAsync(PostReportRequestDto reportDto, Guid reporterId);
        Task<Result<PostReportResponseDto>> ReportPostWithDetailsAsync(PostReportRequestDto reportDto, Guid reporterId);


        Task<Result<IEnumerable<PostReportResponseDto>>> GetAllReportsAsync();
        Task<Result> ResolveReportAsync(Guid reportId);
    }
}
</file>

<file path="Sohba/Controllers/DashboardController.cs">
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.RateLimiting;
using Sohba.Application.DTOs.UserAggregate;
using Sohba.Application.Interfaces;
using Sohba.Domain.Common;
using Sohba.ViewModels.Dashboard;

namespace Sohba.Controllers
{
    [Authorize(Roles = "Admin")]
    [EnableRateLimiting("Dashboard")]
    public class DashboardController : BaseController
    {
        private readonly IUserService _userService;
        private readonly IPostService _postService;
        private readonly IGroupService _groupService;
        private readonly IPageService _pageService;
        private readonly IReportingService _reportingService;
        private readonly IFriendshipService _friendshipService;

        public DashboardController(
            IUserService userService,
            IPostService postService,
            IGroupService groupService,
            IPageService pageService,
            IReportingService reportingService,
            IFriendshipService friendshipService)
        {
            _userService = userService;
            _postService = postService;
            _groupService = groupService;
            _pageService = pageService;
            _reportingService = reportingService;
            _friendshipService = friendshipService;
        }


        public async Task<IActionResult> Index()
        {
            var viewModel = new DashboardViewModel();


            var users = await _userService.GetAllUsersAsync();
            var posts = await _postService.GetAllPostsAsync();
            var groups = await _groupService.GetAllGroupsAsync();
            var pages = await _pageService.GetAllPagesAsync();
            var reports = await _reportingService.GetAllReportsAsync();

            viewModel.TotalUsers = users.Value?.Count() ?? 0;
            viewModel.TotalPosts = posts.Value?.Count() ?? 0;
            viewModel.TotalGroups = groups.Value?.Count() ?? 0;
            viewModel.TotalPages = pages.Value?.Count() ?? 0;
            viewModel.PendingReports = reports.Value?.Count(r => !r.IsResolved) ?? 0;


            viewModel.RecentUsers = users.Value?
                .OrderByDescending(u => u.CreatedAt)
                .Take(5)
                .ToList() ?? new();


            viewModel.RecentPosts = posts.Value?
                .OrderByDescending(p => p.CreatedAt)
                .Take(5)
                .ToList() ?? new();


            viewModel.RecentReports = reports.Value?
                .Where(r => !r.IsResolved)
                .OrderByDescending(r => r.ReportedAt)
                .Take(5)
                .ToList() ?? new();


            viewModel.UsersLast7Days = new List<int> { 5, 8, 12, 7, 15, 10, 20 };
            viewModel.Last7DaysLabels = new List<string> {
                DateTime.Now.AddDays(-6).ToString("MMM dd"),
                DateTime.Now.AddDays(-5).ToString("MMM dd"),
                DateTime.Now.AddDays(-4).ToString("MMM dd"),
                DateTime.Now.AddDays(-3).ToString("MMM dd"),
                DateTime.Now.AddDays(-2).ToString("MMM dd"),
                DateTime.Now.AddDays(-1).ToString("MMM dd"),
                DateTime.Now.ToString("MMM dd")
            };

            return View(viewModel);
        }



        [HttpGet]
        public async Task<IActionResult> Users(string search = "", string status = "all", int page = 1)
        {
            var viewModel = new DashboardUsersViewModel
            {
                SearchTerm = search,
                StatusFilter = status,
                CurrentPage = page,
                PageSize = 20
            };

            Result<IEnumerable<UserResponseDto>> usersResult;

            if (status == "active" || status == "blocked")
            {
                usersResult = await _userService.GetUsersByStatusAsync(status);
            }
            else
            {
                usersResult = await _userService.GetAllUsersAsync();
            }

            if (usersResult.IsSuccess)
            {
                var query = usersResult.Value.AsQueryable();


                if (!string.IsNullOrWhiteSpace(search))
                {
                    query = query.Where(u =>
                        u.Name.Contains(search, StringComparison.OrdinalIgnoreCase) ||
                        u.Email.Contains(search, StringComparison.OrdinalIgnoreCase));
                }

                viewModel.TotalCount = query.Count();
                viewModel.Users = query
                    .OrderByDescending(u => u.CreatedAt)
                    .Skip((page - 1) * viewModel.PageSize)
                    .Take(viewModel.PageSize)
                    .ToList();
            }

            return View(viewModel);
        }

        [HttpPost]
        public async Task<IActionResult> BlockUser(Guid userId)
        {
            var result = await _friendshipService.BlockUserAsync(GetCurrentUserId(), userId);
            return Json(new { success = result.IsSuccess, error = result.Error });
        }

        [HttpPost]
        public async Task<IActionResult> UnblockUser(Guid userId)
        {
            var result = await _friendshipService.UnblockUserAsync(GetCurrentUserId(), userId);
            return Json(new { success = result.IsSuccess, error = result.Error });
        }

        [HttpPost]
        public async Task<IActionResult> DeleteUser(Guid userId)
        {
            var result = await _userService.DeleteUserAsync(userId);
            return Json(new { success = result.IsSuccess, error = result.Error });
        }



        [HttpGet]
        public async Task<IActionResult> Posts(string search = "", string source = "all", int page = 1)
        {
            var viewModel = new DashboardPostsViewModel
            {
                SearchTerm = search,
                SourceFilter = source,
                CurrentPage = page,
                PageSize = 20
            };

            var postsResult = await _postService.GetAllPostsAsync();
            if (postsResult.IsSuccess)
            {
                var query = postsResult.Value.AsQueryable();


                if (!string.IsNullOrWhiteSpace(search))
                {
                    query = query.Where(p =>
                        p.Title.Contains(search, StringComparison.OrdinalIgnoreCase) ||
                        p.Content.Contains(search, StringComparison.OrdinalIgnoreCase));
                }


                if (source != "all")
                {
                    query = query.Where(p => p.SourceType.Equals(source, StringComparison.OrdinalIgnoreCase));
                }

                viewModel.TotalCount = query.Count();
                viewModel.Posts = query
                    .OrderByDescending(p => p.CreatedAt)
                    .Skip((page - 1) * viewModel.PageSize)
                    .Take(viewModel.PageSize)
                    .ToList();
            }

            return View(viewModel);
        }

        [HttpPost]
        public async Task<IActionResult> DeletePost(Guid postId)
        {
            var result = await _postService.DeletePostAsync(postId, GetCurrentUserId());
            return Json(new { success = result.IsSuccess, error = result.Error });
        }

        [HttpPost]
        public async Task<IActionResult> HidePost(Guid postId)
        {
            var result = await _postService.HidePostAsync(postId, GetCurrentUserId());
            return Json(new { success = result.IsSuccess, error = result.Error });
        }



        [HttpGet]
        public async Task<IActionResult> Reports(string status = "pending", int page = 1)
        {
            var viewModel = new DashboardReportsViewModel
            {
                StatusFilter = status,
                CurrentPage = page,
                PageSize = 20
            };

            var reportsResult = await _reportingService.GetAllReportsAsync();
            if (reportsResult.IsSuccess)
            {
                var query = reportsResult.Value.AsQueryable();


                if (status == "pending")
                {
                    query = query.Where(r => !r.IsResolved);
                }
                else if (status == "resolved")
                {
                    query = query.Where(r => r.IsResolved);
                }

                viewModel.TotalCount = query.Count();
                viewModel.Reports = query
                    .OrderByDescending(r => r.ReportedAt)
                    .Skip((page - 1) * viewModel.PageSize)
                    .Take(viewModel.PageSize)
                    .ToList();
            }

            return View(viewModel);
        }

        [HttpPost]
        public async Task<IActionResult> ResolveReport(Guid reportId)
        {
            var result = await _reportingService.ResolveReportAsync(reportId);
            return Json(new { success = result.IsSuccess, error = result.Error });
        }

        [HttpPost]
        public async Task<IActionResult> DismissReport(Guid reportId)
        {

            var result = await _reportingService.ResolveReportAsync(reportId);
            return Json(new { success = result.IsSuccess, error = result.Error });
        }

        [HttpPost]
        public async Task<IActionResult> DeleteReportedPost(Guid postId, Guid reportId)
        {
            var deleteResult = await _postService.DeletePostAsync(postId, GetCurrentUserId());
            if (deleteResult.IsSuccess)
            {
                await _reportingService.ResolveReportAsync(reportId);
            }
            return Json(new { success = deleteResult.IsSuccess, error = deleteResult.Error });
        }



        [HttpGet]
        public async Task<IActionResult> GetUserDetails(Guid userId)
        {
            var result = await _userService.GetProfileAsync(userId);
            if (result.IsSuccess)
            {
                return PartialView("Partials/_UserDetails", result.Value);
            }
            return Content("User not found");
        }

        [HttpGet]
        public async Task<IActionResult> GetPostDetails(Guid postId)
        {
            var result = await _postService.GetPostByIdAsync(postId, GetCurrentUserId());
            if (result.IsSuccess)
            {
                return PartialView("Partials/_PostDetails", result.Value);
            }
            return Content("Post not found");
        }

        [HttpGet]
        public async Task<IActionResult> GetReportDetails(Guid reportId)
        {
            var reports = await _reportingService.GetAllReportsAsync();
            var report = reports.Value?.FirstOrDefault(r => r.Id == reportId);
            if (report != null)
            {
                return PartialView("Partials/_ReportDetails", report);
            }
            return Content("Report not found");
        }
    }
}
</file>

<file path="Sohba.Application/DTOs/PostAggregate/PostResponseDto.cs">
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Application.DTOs.PostAggregate
{
    public class PostResponseDto
    {
        public Guid Id { get; set; }
        public string Title { get; set; }
        public string Content { get; set; }
        public string? ImageUrl { get; set; }
        public string AuthorName { get; set; }
        public int CommentsCount { get; set; }
        public int ReactionsCount { get; set; }
        public DateTime CreatedAt { get; set; }
        public string? CurrentUserReaction { get; set; }

        public bool IsSaved { get; set; }
        public string? SavedTag { get; set; }
        public bool IsFavorite { get; set; }

        public bool IsReportedByCurrentUser { get; set; }

        public bool IsAuthor { get; set; }
        public bool IsPrivate { get; set; }
        public string SourceType { get; set; }
        public string? SourceName { get; set; }
        public Guid? SourceId { get; set; }
    }
}
</file>

<file path="Sohba.Application/Interfaces/IUserService.cs">
using Sohba.Application.DTOs.UserAggregate;
using Sohba.Domain.Common;
using System;
using System.Collections.Generic;
using System.Security;
using System.Text;

namespace Sohba.Application.Interfaces
{
    public interface IUserService
    {


        Task<Result<UserResponseDto>> GetProfileAsync(Guid userId);
        Task<Result<bool>> UpdateProfileAsync(Guid userId, UserRequestDto updateDto);


        Task<Result<UserResponseDto>> GetProfileAsync(Guid userId, Guid currentUserId);


        Task<Result<IEnumerable<UserResponseDto>>> GetAllUsersAsync();
        Task<Result<bool>> DeleteUserAsync(Guid userId);
        Task<Result<IEnumerable<UserResponseDto>>> GetUsersByStatusAsync(string status);
    }
}
</file>

<file path="Sohba.Application/Interfaces/IPostService.cs">
using Sohba.Application.DTOs.Common;
using Sohba.Application.DTOs.PostAggregate;
using Sohba.Domain.Common;
using Sohba.Domain.Entities.PostAggregate;
using System;
using System.Collections.Generic;
using System.Text;

namespace Sohba.Application.Interfaces
{
    public interface IPostService
    {

        Task<Result<PagedResult<PostResponseDto>>> GetFeedAsync(
            Guid userId,
            int page = 1,
            int pageSize = 10);



        Task<Result<IEnumerable<PostResponseDto>>> GetFeedAsync(Guid userId);
        Task<Result<PostResponseDto>> CreatePostAsync(PostCreateDto postDto, Guid userId);
        Task<Result<PostResponseDto>> GetPostByIdAsync(Guid postId, Guid currentUserId);
        Task<Result> DeletePostAsync(Guid postId, Guid userId, bool isAdmin = false);
        Task<Result> UpdatePostAsync(Guid postId, PostUpdateDto postDto, Guid userId);


        Task<Result<IEnumerable<PostResponseDto>>> GetGroupPostsAsync(Guid groupId, Guid currentUserId);
        Task<Result<IEnumerable<PostResponseDto>>> GetPagePostsAsync(Guid pageId, Guid currentUserId);
        Task<Result<IEnumerable<PostResponseDto>>> GetUserPostsAsync(Guid userId, Guid currentUserId);


        Task<Result<IEnumerable<PostResponseDto>>> GetAllPostsAsync();
        Task<Result> HidePostAsync(Guid postId, Guid userId);

        Task<Result<IEnumerable<PostResponseDto>>> MapPostsWithInteractions(IEnumerable<Post> posts, Guid currentUserId);

    }
}
</file>

</files>
